{
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "node": "^18.9.1",
    "nodemon": "^2.0.20",
    "dotenv": "^16.0.0"
  },
  "scripts" : {
    "start": "node app.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  }
}